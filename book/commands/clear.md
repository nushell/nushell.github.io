---
title: clear
version: 0.69.1
platform: |
  Clear the terminal.
usage: |
  Clear the terminal.
---

# <code>{{ $frontmatter.title }}</code> for platform

<div class='command-title'>{{ $frontmatter.platform }}</div>

## Signature

```> clear ```

## Examples

Clear the terminal
```shell
> clear
```
